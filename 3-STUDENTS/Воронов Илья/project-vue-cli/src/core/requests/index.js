export function get(url) {
    return fetch(url).then((d) => d.json());
}

export function post() {
    //   return fetch(url).then((d) => d.json());
}

export function put() {
    //   return fetch(url).then((d) => d.json());
}

export function deleteReq() {
    //   return fetch(url).then((d) => d.json());
}